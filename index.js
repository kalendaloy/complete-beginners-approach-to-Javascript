const express = require('express')

const app=express();

const port = 20007;

//app.listenportNumber,function)
app.listen(port, ()=> {
                        console.log('listening to port 20007')
                         })